// Costanti condivise non legate a "use server".

/** Cookie che memorizza l'istante dell'ultima lettura delle notifiche manager. */
export const NOTIF_SEEN_COOKIE = "notif_seen";

/** Massimo ore di lavoro per una segretaria in un singolo giorno. */
export const MAX_ORE_GIORNO = 6.5;

/** Durata minima di un turno proposto dalla bozza automatica (niente miniturni). */
export const MIN_ORE_TURNO = 3;
